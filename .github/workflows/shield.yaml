name: "Zephyr Zest_Adapter_Click CI/CD"

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  board:
    uses: catie-aq/zephyr_workflows/.github/workflows/shield.yml@main
    with:
      shield: "zest_adapter_click"

    secrets:
      personal_access_token: ${{ secrets.CI_6TRON_ZEPHYR_RO }}
